Backend Let’s make our app functional and integrate the backend part: Auth Database API For all of that, we will use Supabase - an open-source alternative to Firabase. What I like about Supabase is that it is built on top of Postgres wich makes it super powerful. Let’s follow this guide to setup and integrate Supabase in React Native: SupabaseBuild a User Management App with Expo React Native | Supabas…​Setup Supabase Create a new Supabase project Setup the database schema Install supbase-js in our project Setup the .env keys Auth Create the Auth screens Setup an Auth Context to share data about auth status Auth flow: redirect users to the right screens and protect the app from unauthorised users Profile page Show profile details Update profile info (Optional) Avatar Events Create the event schema Populate the database with some data SQL Insert statements INSERT INTO events (id, title, description, date, location, image_uri) VALUES (1, 'React Native Workshop', 'Learn the basics of React Native and build your first app.', '2024-10-10T14:00:00+00:00', 'Tech Hub, Barcelona', 'https://notjustdev-dummy.s3.us-east-2.amazonaws.com/images/1.jpg'); INSERT INTO events (id, title, description, date, location, image_uri) VALUES (2, 'Supabase for Beginners', 'A beginners guide to Supabase and how to integrate it with your apps.', '2024-10-11T16:00:00+00:00', 'Online', 'https://notjustdev-dummy.s3.us-east-2.amazonaws.com/images/2.jpg'); ​Fetch events in the app RSPV - attendance Add a User → Event many-to-many relationship to track attendance Create attendance Read attendance Fetch all users attending an event Part 2 Create events At this moment, we have only added events directly in the database. It would be better to allow users to add events. For that, create a form, and save a new event in the database. Storage Implement supabase storage and upload images to the cloud. Avatars Event Images Geo queries Store the location of events, and fetch only nearby locations. SupabasePostGIS: Geo queries | Supabase Docs​Nearby Events database function CREATE OR REPLACE FUNCTION nearby_events(lat FLOAT, long FLOAT) RETURNS TABLE ( id public.events.id%TYPE, created_at public.events.created_at%TYPE, title public.events.title%TYPE, description public.events.description%TYPE, date public.events.date%TYPE, location public.events.location%TYPE, image_uri public.events.image_uri%TYPE, user_id public.events.user_id%TYPE, lat FLOAT, long FLOAT, dist_meters FLOAT ) LANGUAGE sql AS $$ SELECT id, created_at, title, description, date, location, image_uri, user_id, ST_Y(location_point::geometry) AS lat, ST_X(location_point::geometry) AS long, ST_Distance(location_point, ST_Point(long, lat)::geography) AS dist_meters FROM public.events ORDER BY location_point <-> ST_Point(long, lat)::geography; $$;